@model IEnumerable<NutriHub.Models.PadecimientoPaciente>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.fechaDiagnostico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paciente1.ocupacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Padecimiento.nombre)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.fechaDiagnostico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paciente1.ocupacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Padecimiento.nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
